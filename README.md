# PatternForge
PatternForge is a C console application that generates scalable star-based alphabet patterns for any user-input string. Enter the size and text, and watch each character render as a structured pattern. A fun project showcasing loops, functions, and string handling in C.
